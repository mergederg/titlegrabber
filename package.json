{
  "name": "titlegrabber",
  "version": "1.0.0",
  "description": "Take a sitemap xml csv, fetch all the URLs one by one, add the titles to the rows and save the CSV output.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "type": "module",
  "author": "Vesper",
  "license": "ISC",
  "dependencies": {
    "csvtojson": "^2.0.10",
    "export-to-csv": "^0.2.1",
    "fs": "^0.0.1-security",
    "get-title-at-url": "^2.0.0"
  }
}
